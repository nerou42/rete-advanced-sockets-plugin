{
  "name": "rete-advanced-sockets-plugin",
  "version": "0.1.7",
  "description": "A plugin for formula to extend the default sockets",
  "scripts": {
    "build": "tsup ./src/index.ts --dts --format esm",
    "lint": "rete lint"
  },
  "author": "",
  "license": "MIT",
  "peerDependencies": {
    "rete": "^2.0.0"
  },
  "devDependencies": {
    "rete": "^2.0.0",
    "rete-cli": "^1.0.3",
    "typescript": "4.8.4",
    "tsup": "^8.2.3"
  },
  "main": "./dist/index.mjs",
  "types": "./dist/index.d.mts"
}
